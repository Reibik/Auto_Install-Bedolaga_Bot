# Changelog

Все значимые изменения в проекте ST Village будут документированы в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [19.0] - 2026-04-05

### Изменено (Рефакторинг)
- **Размер скрипта уменьшен на 21%** — с 2313 до 1819 строк без потери функциональности
- **Компактные разделители секций** — `# === SECTION ===` вместо 3-строчных блоков
- **`_container_state()` + `_state_icon()`** — общие хелперы для docker inspect, заменили дублированный код в 4 местах (check_health_quick, health_check_full, run_cron_healthcheck, run_cron_watchdog)
- **`_get_ver_txt()`** — единая функция проверки git-версий с режимами "full" (git fetch) и "fast" (local only), заменила дублирование в check_versions и check_versions_cached
- **Слияние `check_ports` / `check_ports_before_install`** — одна функция `check_ports()` с параметром mode ("install"/"normal")
- **`_resolve_domain()`** — общий DNS-резолвер, используется в verify_domains_resolve и check_dns
- **`_menu_header()` / `_menu_choice()`** — дедупликация шапки меню и промпта выбора из 7+ мест
- **`_cron_toggle()`** — универсальная функция enable/disable cron для backup, healthcheck, watchdog (заменила 6 отдельных функций)
- **`_ssl_days_left()`** — общая SSL-проверка для check_ssl_certs и run_cron_healthcheck
- **`_compose_stop_all()` / `_compose_down_all()`** — остановка/удаление всех сервисов из 3 мест
- **`_prompt_env()`** — универсальный промпт для env wizard
- **`_flag_status()`** — on/off статус флагов одной строкой
- **`_ensure_env()`** — .env инициализация из .env.example

### Удалено
- `press_any_key_to_continue()` — dead code (был алиасом для `pause()`)
- `enable_auto_backup()` / `disable_auto_backup()` — заменены на `_cron_toggle()`
- `enable_cron_healthcheck()` / `disable_cron_healthcheck()` — заменены на `_cron_toggle()`
- `enable_watchdog()` / `disable_watchdog()` — заменены на `_cron_toggle()`
- `check_ports_before_install()` — слита в `check_ports()`

---

## [18.0] - 2026-04-05

### Добавлено
- **Cron Health Check** — автоматическая проверка сервисов каждые 10 минут с Telegram-уведомлениями при проблемах (контейнеры, HTTPS, SSL)
- **Container Watchdog** — авто-рестарт упавших контейнеров каждые 5 минут с Telegram-уведомлениями
- **DNS-валидация перед запуском Caddy** — проверка A-записей доменов перед стартом, предотвращение проблем с SSL-сертификатами
- **Логирование в файл** — все операции записываются в `st_village.log` с автоматической ротацией при 10 МБ
- **Кэширование версий** — быстрая загрузка дашборда без `git fetch` (кэш 5 мин.), отдельная кнопка "Проверить обновления"
- **Просмотр лога панели** — новый пункт меню для просмотра последних записей лога
- `CONTRIBUTING.md` — руководство по участию в проекте
- `SECURITY.md` — политика безопасности и ответственного раскрытия
- `.github/workflows/security.yml` — Security Scan workflow (ShellCheck аудит, поиск секретов, проверка безопасности)

### Изменено
- Главное меню расширено: добавлен пункт "Проверить обновления компонентов" (11)
- Меню "Система и безопасность" расширено: добавлена секция "Мониторинг" (Health Check, Watchdog, Лог)
- `start_project()` теперь проверяет DNS перед запуском Caddy
- `main()` использует `case` вместо цепочки `if` для cron-аргументов
- `init_tty()` поддерживает `cron_watchdog` аргумент

### Исправлено (ShellCheck)
- SC2164: добавлен `|| exit 1` к `cd` в подоболочках
- SC2086: закавычены `${HTTP_PORT}` и `${HTTPS_PORT}` в `for` циклах
- SC2086: закавычен `return "$has_conflicts"`
- SC2116: заменён `echo | tr -d ' '` на parameter expansion `${var// /}`
- Устранены предсказуемые временные файлы (`/tmp/stv_*.$$` → `mktemp`)
- Устранены мёртвые записи переменных в подоболочках (check_versions)

---

## [17.0] - 2026-04-05

### Добавлено
- `.gitignore` для исключения временных и системных файлов
- `CHANGELOG.md` для отслеживания версий и изменений
- `LICENSE` файл (MIT License)
- Улучшенная структура проекта с директориями `docs/` и `examples/`
- Проверка занятости портов 80/443 перед установкой
- Конфигурируемые пути установки (не только `/root`)
- Улучшенная обработка ошибок при занятых портах

### Изменено
- Рефакторинг конфигурационного блока для лучшей читаемости
- Оптимизация функций проверки портов
- Улучшена документация в README.md

### Исправлено
- Проблема с установкой при занятых портах другими сервисами

---

## [16.0] - 2026-04-04

### Добавлено
- Интерактивные мастера настройки `.env` с валидацией
- Диагностика сервисов: health check, DNS, порты, SSL
- Безопасность: UFW файрвол, Fail2ban
- Telegram-уведомления о событиях
- Восстановление из бэкапов
- Миграция между серверами
- Управление Swap
- Полное удаление проекта
- Автоматическая генерация JWT-секретов при установке

### Изменено
- Улучшена панель управления с цветным выводом
- Расширена система бэкапов с ротацией

---

## [Предыдущие версии]

История версий до 16.0 не документирована.
